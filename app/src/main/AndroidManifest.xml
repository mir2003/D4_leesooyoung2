<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hs20100220.d4_leesooyoung2" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MenuActivity"
                  android:label="@string/title_menu">
        </activity>

        <activity android:name=".CustActivity"
            android:label="@string/title_cust">
        </activity>
        <activity android:name=".SaleActivity"
            android:label="@string/title_sale">
        </activity>
        <activity android:name=".ProdActivity"
            android:label="@string/title_prod">
        </activity>
    </application>

</manifest>
